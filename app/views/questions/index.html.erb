<div class="container">
    <h1 class="title-lg mb">Questions</h1>

    <% @questions.each do |question| %>
        <p class="mb-sm"><%= link_to "Question #{question.id}", question_path(question) %></p>
    <% end %>

    <p class="mt">
        <%= link_to "Create new question", new_question_path,
         class: 'button button-main' %>
    </p>
</div>